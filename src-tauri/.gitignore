# Auto-generated by tauri-build on every compile (Tauri 2 capability /
# ACL schema artifacts). Reproducible from tauri.conf.json + capabilities/
# at build time, so checking them in would just churn the diff.
/gen/
/target/
